import  cn.milai.ib.character.plane.PlayerPlane;
import  cn.milai.ib.character.plane.WelcomePlane;
import  cn.milai.ib.component.form.DramaDialog;
import  cn.milai.ib.character.plane.FollowPlane;
import  cn.milai.ib.character.plane.MissileBoss;

# 这是华丽的注释
cn.milai.ib.demo.drama.Hello("测试用的剧本") {
	# 这条注释更华丽
	void main() {
	    add DramaDialog(277, 517, img("/img/speaker.gif"), "new_feature_about_dialog");
	    add WelcomePlane(222, 0);
	    add WelcomePlane(277, -138);
	    sleep(10);
################### 你见过这么帅的注释吗  ##########################
	    add FollowPlane(111, 0);
	    add FollowPlane(444, 0);
	    sleep(20);
	    add MissileBoss(277, -67);
	}
}